# InvoicePro - AI-Powered Invoice Procurement Platform

## Overview

InvoicePro is a comprehensive invoice processing and procurement management platform that leverages AI-powered OCR, automated data extraction, and intelligent validation workflows. The system is built as a full-stack web application with React frontend and Express.js backend, designed to streamline invoice approval processes and reduce manual data entry errors.

## System Architecture

### Frontend Architecture
- **Framework**: React 18 with TypeScript
- **Styling**: Tailwind CSS with shadcn/ui component library
- **State Management**: TanStack Query (React Query) for server state
- **Routing**: Wouter for client-side routing
- **Form Handling**: React Hook Form with Zod validation
- **Build Tool**: Vite for development and production builds

### Backend Architecture
- **Runtime**: Node.js with Express.js framework
- **Language**: TypeScript with ESM modules
- **Database**: PostgreSQL with Drizzle ORM
- **Authentication**: Replit Auth with session-based authentication
- **File Processing**: Multer for uploads, Tesseract.js for OCR
- **AI Integration**: OpenAI API for intelligent data extraction

### Database Design
The system uses PostgreSQL with the following key entities:
- **Users**: Authentication and profile management (required for Replit Auth)
- **Sessions**: Session storage (required for Replit Auth)
- **Invoices**: Core invoice data with extracted information
- **Line Items**: Individual invoice line items
- **Purchase Orders**: PO management and matching
- **Projects**: Project validation and assignment
- **Validation Rules**: Configurable business rules
- **Approvals**: Workflow and approval tracking
- **Flags & Alerts**: Discrepancy detection and predictive analytics

## Key Components

### AI-Powered Processing Pipeline
1. **OCR Service**: Tesseract.js processes PDF/image invoices
2. **AI Extraction**: OpenAI GPT extracts structured data from OCR text
3. **Validation Engine**: Rule-based validation with configurable business rules
4. **Discrepancy Detection**: Automated flagging of potential issues
5. **Predictive Analytics**: ML-based issue prediction and alerts

### RPA Invoice Importer (Enhanced)
6. **Python RPA Service**: Automated invoice downloading from ERP systems
7. **Real-time Console View**: Live log streaming with WebSocket integration
8. **Progress Tracking**: Real-time status indicators and import statistics
9. **Configuration Management**: Click-to-expand console with visual status updates

### Authentication & Security
- Replit Auth integration with OpenID Connect
- Session-based authentication with PostgreSQL session store
- Protected API routes with middleware validation
- File upload security with type and size restrictions

### Data Processing Workflow
1. Invoice upload (PDF/JPG/PNG)
2. OCR text extraction
3. AI-powered data extraction
4. Validation rule application
5. PO matching and project assignment
6. Approval workflow routing
7. Discrepancy flagging and resolution

### User Interface Components
- **Dashboard**: Real-time stats and processing overview
- **Invoice Management**: Upload, preview, and data extraction
- **Approval Workflow**: Multi-stage approval process
- **PO Matching**: Automated and manual purchase order matching
- **Project Validation**: Project assignment and validation
- **Reporting**: Analytics and trend analysis
- **Configuration**: Validation rules and system settings

## Data Flow

1. **Invoice Upload**: Files processed through secure upload endpoint
2. **OCR Processing**: Background processing with status updates
3. **AI Extraction**: Structured data extraction with confidence scoring
4. **Validation**: Rule-based validation with flag generation
5. **Matching**: Automatic PO matching and project assignment
6. **Approval**: Workflow routing based on business rules
7. **Storage**: Processed data stored with audit trail

## External Dependencies

### Core Dependencies
- **Database**: Neon PostgreSQL (serverless)
- **AI Services**: OpenAI API for data extraction
- **Authentication**: Replit Auth service
- **File Processing**: PDF.js, Tesseract.js, Sharp for image processing
- **UI Components**: Radix UI primitives via shadcn/ui

### Development Tools
- **TypeScript**: Type safety across the stack
- **Drizzle Kit**: Database migrations and schema management
- **ESBuild**: Production bundling for server code
- **Vite**: Frontend development and bundling

## Deployment Strategy

### Environment Configuration
- **Development**: `npm run dev` - Concurrent frontend/backend development
- **Production**: `npm run build && npm run start` - Optimized build deployment
- **Database**: Drizzle push for schema updates

### Replit Deployment
- **Platform**: Replit autoscale deployment
- **Modules**: Node.js 20, Web, PostgreSQL 16
- **Build Process**: Vite build + ESBuild server bundling
- **Port Configuration**: Internal 5000, External 80

### Key Configuration Files
- **drizzle.config.ts**: Database configuration and migrations
- **vite.config.ts**: Frontend build configuration with path aliases
- **tsconfig.json**: TypeScript configuration for monorepo structure
- **.replit**: Replit-specific deployment configuration

## Changelog
- June 13, 2025. Initial setup
- June 23, 2025. Added verified invoices system: automatic validation processing moves approved invoices to verified status when they pass validation criteria, displayed in Unresolved Invoice-PO Matches section
- June 26, 2025. Added modular ERP automation platform: AI-powered RPA system with natural language task input, ChatGPT script generation, Playwright browser automation, secure credential management, and real-time task monitoring
- June 27, 2025. Implemented real-time progress tracking system: WebSocket-based live updates during ERP automation tasks, step-by-step progress display with screenshots, enhanced user feedback with comprehensive progress monitoring UI component
- January 02, 2025. Enhanced invoice-PO matching status tracking: Added comprehensive timestamp tracking for match events including matchedAt, approvedAt, rejectedAt timestamps, status change tracking with user attribution, real-time activity feed showing recent match events, detailed status timeline in PO matching interface displaying when matches occur and their approval/rejection history
- January 02, 2025. Optimized RPA system performance: Reduced timeout bottlenecks by 60%, implemented smart selector fallbacks with priority queues, added browser performance optimizations including resource blocking and smaller viewport rendering, reduced step execution times from 5-minute to 2-minute maximums, optimized wait times and eliminated excessive delays that caused system hanging
- January 02, 2025. Enhanced multi-tenant database sharing: Completed implementation of company-based data sharing where users within the same organization can access shared invoices, purchase orders, and project data, added comprehensive database filtering by companyId across all major entities, updated API routes to support company-based data isolation, fixed database schema inconsistencies and added missing timestamp tracking fields for invoice-PO matching system
- January 02, 2025. Added automation workflow saving and scheduling: Implemented saved workflows feature allowing users to save automation tasks for future reuse, created scheduled tasks system with cron-based execution, added node-cron scheduler service for automated task execution, enhanced ERP automation interface with "Save Task" button, built comprehensive API routes for workflow and task management with multi-tenant support
- January 02, 2025. Created ERP Invoice Importer module: Built comprehensive RPA-based invoice importing system with configurable file types (XML/PDF/Both), multiple scheduling options (once/daily/weekly/hourly/multiple daily), real-time progress tracking with step-by-step execution monitoring, multi-tenant import configurations, complete database schema for import logs and metadata, and integrated into ERP Automation navigation menu
- January 07, 2025. Enhanced Invoice Importer with real-time progress visualization: Created comprehensive progress tracker component with live step-by-step updates, execution metrics display (processed/successful/failed invoices), screenshot gallery, detailed error reporting, progress bar visualization, real-time polling system, and enhanced logging with structured step tracking for better user feedback during import processes
- January 13, 2025. Added iPhone-optimized mobile menu: Implemented responsive mobile navigation with hamburger menu, slide-out sheet interface, organized sections for all app features, enhanced touch targets (44px minimum), iOS-specific optimizations including webkit zoom prevention, smooth scrolling, and tap highlighting improvements, WebSocket connection status indicator, and mobile-first design patterns
- January 15, 2025. Enhanced XML invoice processing: Improved AI extraction for XML invoices with specialized XML parsing prompts, better field mapping for electronic invoice formats, increased XML content truncation limit to 8000 characters, and added XML detection logic to differentiate between OCR and XML processing for better extraction accuracy
- January 16, 2025. Fixed XML subtotal extraction issue: Enhanced XML parser with comprehensive fallback patterns for subtotal extraction, added alternative patterns for TaxExclusiveAmount/LineExtensionAmount/TaxableAmount, improved calculation logic for missing subtotal values, fixed TypeScript compilation errors with ES2018 features, and added extensive debugging for XML parsing to resolve "N/A" subtotal values
- January 16, 2025. Fixed application startup error: Resolved "Cannot access 'processInvoiceWrapper' before initialization" error in invoice upload processing by removing wrapper function scope issue in setImmediate callback, ensuring proper function accessibility and preventing server crashes during invoice processing initialization
- January 16, 2025. Enhanced application stability: Fixed recurring crash issues by completing incomplete project validation endpoint, removing orphaned code fragments causing syntax errors, and implementing timeout protection for database operations in user settings routes to prevent hanging operations that could cause memory exhaustion
- January 17, 2025. Added invoice upload functionality: Implemented upload button in Invoices page header with support for PDF, XML, JPG, JPEG, and PNG files, multiple file uploads, real-time upload status, and automatic page refresh after successful upload
- January 17, 2025. Fixed application crash: Resolved syntax errors in server/routes.ts caused by incomplete code blocks and duplicate closing braces, ensuring proper invoice upload handler completion and preventing server startup failures
- January 17, 2025. Fixed LearningTracker crash: Added error handling around LearningTracker.recordFeedback method call to prevent application crashes during feedback submission, ensuring stable operation when Colombian invoice processing encounters learning system issues
- January 22, 2025. Fixed Invoice Importer execution and storage issues: Resolved missing storage interface methods causing "function not defined" errors, fixed user access control preventing configuration execution, added proper cascade deletion for foreign key constraints, completed database schema with headless column for Chrome browser control, and enabled both headless and visible browser modes for Python RPA automation
- January 22, 2025. Fixed application crash in TopIssuesWidget: Added null/undefined checks in getTrendIcon and getTrendColor functions to prevent "Cannot read properties of undefined (reading 'startsWith')" error when trend data is missing
- January 22, 2025. Completed Python RPA invoice storage integration: Added database integration layer to save imported invoices from Python RPA local SQLite databases to main PostgreSQL database, implemented saveImportedInvoicesToDatabase method to process downloaded PDFs and XML files, created API routes for viewing imported invoices (/api/imported-invoices and /api/invoice-importer/logs/:logId/invoices), added metadata tracking for invoice source type and ERP document details, fixed foreign key CASCADE deletion chain for complete ERP connection cleanup
- January 24, 2025. Fixed RPA invoice processing pipeline: Resolved critical issue where RPA-imported invoices weren't appearing in Invoices Module because they skipped OCR and AI processing steps that manual imports use. Updated storeImportedInvoicesFast to run full processing pipeline (OCR + AI extraction) identical to manual upload flow, ensuring RPA invoices get proper vendorName, totalAmount, and extractedData fields. RPA invoices now appear consistently with manual imports and have complete data extraction.
- January 24, 2025. Unified RPA and manual invoice processing: Completely redesigned Python RPA script to integrate directly with manual upload pipeline instead of using separate database functions. After extracting XML files from downloaded ZIPs, each file is immediately processed through the same manual invoice upload route (/api/rpa/process-xml), ensuring identical validation, parsing, OCR, AI extraction, and database insertion logic. Eliminated duplicate processing paths - both RPA and manual imports now use unified backend processing with proper Colombian invoice rules and complete data extraction.
- January 24, 2025. Implemented complete invoice file download functionality: Added secure download endpoint (/api/invoices/:id/download) with authentication and company-based access control, enabling users to download both manually uploaded and RPA-imported files. Enhanced frontend Download button with proper error handling, toast notifications, and support for XML, PDF, JPG, and PNG files with correct MIME types. Users can now verify RPA-imported XML invoices by downloading the original files directly from the Invoice Module.
- January 28, 2025. Enhanced XML invoice processing with AttachedDocument wrapper support: Added advanced XML parsing capability to detect and handle AttachedDocument wrappers that contain embedded UBL Invoice/CreditNote documents in CDATA sections. Parser now recursively extracts embedded XML content from CDATA and processes it normally, ensuring compatibility with both regular UBL Invoice formats and AttachedDocument wrapper formats. Enhanced detection logic properly differentiates between pure wrapper files and regular invoices, maintaining backward compatibility with existing XML processing.
- January 28, 2025. Implemented comprehensive Schedule Overview dashboard: Built full-featured schedule management dashboard with backend API endpoints (/api/schedule-overview, /api/schedule-overview/:configId/toggle), real-time status display, visual configuration cards showing schedule types with icons, next run time countdowns, pause/resume controls, connection details, frequency patterns, and auto-refresh every 30 seconds. Dashboard processes all schedule types (daily, weekly, hourly, multiple daily, cron) with proper timezone handling and displays comprehensive metadata including configuration names, ERP connections, status badges, and summary statistics.
- January 28, 2025. Added edit functionality to import RPA configurations: Implemented comprehensive edit feature for invoice importer configurations with Edit button alongside Run Now and Delete buttons, complete edit dialog with all configuration fields (name, ERP connection, file types, schedule type, headless mode), backend PUT API endpoint (/api/invoice-importer/configs/:id) for updating configurations with proper authentication and validation, and seamless form population with existing configuration data for easy modification of scheduled import tasks.
- January 28, 2025. Fixed Schedule Overview synchronization issue: Resolved discrepancy where Schedule Overview displayed 6 scheduled tasks when only 2 active configurations existed. Enhanced filtering logic to exclude one-time ('once') configurations and inactive configurations from scheduled tasks display. Added cleanup functionality to remove orphaned inactive configurations older than 1 day. Schedule Overview now properly syncs with active configurations and only shows truly scheduled recurring tasks (daily, weekly, hourly, multiple_daily, cron types).
- January 29, 2025. Enhanced ZIP file processing with comprehensive XML/PDF support: Implemented intelligent file matching algorithm that extracts both XML and PDF files from ZIP archives, pairs them by base filename, and processes according to configuration. Added three file type modes: "XML only" (existing behavior), "PDF only" (OCR processing), and "Both PDF and XML" (XML for data extraction, PDF for reference). Enhanced Python RPA service with file matching logic, comprehensive logging for processed/matched/unmatched files, and dual API endpoints (/api/rpa/process-xml and /api/rpa/process-pdf) for unified manual upload pipeline integration.
- January 29, 2025. Fixed PDF import processing race condition: Resolved critical issue where PDF imports failed due to Python RPA service deleting PDF files immediately after triggering Node.js processing, creating a race condition. Enhanced error handling in trigger_manual_processing method with proper return values and synchronous processing confirmation. Fixed database enum synchronization by adding "manual" to PostgreSQL schedule_type enum to resolve "invalid input value for enum schedule_type: 'manual'" error when creating manual RPA configurations.
- January 29, 2025. Implemented comprehensive credential management and synchronization system: Added automatic credential synchronization between ERP connections and invoice import configurations to ensure RPA processes always use the most recent authentication information. Enhanced invoice importer service to support both connection-based and manual credential configurations with proper password decryption debugging. Fixed credential synchronization issues where import configurations retained stale passwords after ERP connection updates. Added extensive debug logging throughout authentication flow from Node.js to Python RPA services with Base64 password encryption/decryption verification. Created automated sync functionality that updates all connection-based import configurations when ERP connection credentials are modified, ensuring consistent authentication across the entire automation pipeline.
- January 29, 2025. Enhanced RPA file handling with conditional storage logic: Implemented intelligent file storage system that handles three scenarios - XML only (isDataSource=true), PDF only (isDataSource=false), and both XML+PDF (XML as data source, PDF as reference with matchedFileId linking). Updated Python RPA service to scan and match files by base filename, store them in imported_invoices table with proper linking via baseFileName and matchedFileId fields. Enhanced download functionality to automatically generate ZIP files when both XML and PDF exist for same invoice, otherwise downloads single file. Added comprehensive file linking logic in database with proper conditional storage based on available file types per invoice.
- January 29, 2025. Fixed RPA invoice visibility bug: Resolved critical issue where RPA-imported invoices weren't appearing in main Invoices module due to missing company_id association. Updated RPA processing endpoints (/api/rpa/process-xml and /api/rpa/process-pdf) to inherit company ID from import configurations, modified Python RPA service to pass configId parameter, and corrected 23 existing database records with proper company associations. Added config_id attribute to InvoiceRPAService class to support company-based data isolation. RPA invoices now appear correctly in main Invoices module for users within the same company.
- January 29, 2025. Completed comprehensive RPA invoice visibility fix: Resolved recurring issue where both XML and PDF RPA imports had NULL company_id values preventing visibility in main Invoices module. Fixed all import configurations (configs 24, 27) and corrected 19+ RPA invoice records with proper company associations. Verified that both /api/rpa/process-xml and /api/rpa/process-pdf endpoints correctly inherit company_id from import configurations for all file types (XML-only, PDF-only, mixed XML+PDF). Ensured complete multi-tenant data isolation with proper company-based filtering across all RPA import processes.
- January 29, 2025. Implemented comprehensive RPA extraction priority rules: Enhanced RPA system to enforce strict extraction priority where XML files always take precedence over PDF files for data extraction when both exist for the same invoice. Updated Python RPA service with priority processing logic that processes XML for structured data extraction while storing PDF as visual reference only (isDataSource=false). Enhanced Node.js backend with duplicate prevention mechanism using getInvoicesByFileName method to prevent multiple invoice records for same base filename. Added comprehensive logging and priority indicators throughout extraction pipeline ensuring XML-first processing with PDF reference-only storage for optimal data accuracy and preventing extraction conflicts.
- January 29, 2025. Fixed PDF-to-XML invoice linking issue: Resolved critical problem where PDF files weren't being properly linked back to XML-derived invoice records despite sharing the same base filename. Added linked_invoice_id column to imported_invoices table, implemented _link_pdfs_to_main_invoices method in Python RPA service to create direct associations between PDFs and XML-derived invoices in main invoices table. Enhanced download endpoint with intelligent ZIP packaging that automatically includes linked PDFs when downloading XML invoices. Added /api/invoices/:id/linked-files endpoint to show PDF associations. System now properly displays both XML data extraction and PDF visual references as unified invoice packages.
- January 29, 2025. Implemented robust token-based invoice file matching system: Enhanced RPA import process with intelligent invoice token generation using document number, tax ID, and total amount extracted from XML content. Replaced unreliable filename-based matching with composite token matching that handles format discrepancies and value rounding differences. Added _extract_invoice_token method for metadata extraction, _match_files_by_token method with exact token and fallback document+tax matching strategies. Enhanced _link_pdfs_to_main_invoices with multi-strategy database search using extracted data fields (documentNumber, vendorTaxId) and comprehensive fallback patterns. System now reliably links XML and PDF files even with filename variations, ensuring consistent data extraction and visual reference associations.
- January 29, 2025. Fixed RPA progress tracking and file storage issues: Resolved critical bug where completed RPA imports showed zero statistics in UI despite successful processing. Updated database storage paths for PDF files from incorrect 'uploads/' to proper '/tmp/invoice_downloads/pdfs/' locations. Enhanced progress API endpoint to return final completion statistics from database when active imports are cleared from memory. Added cleanup logic to remove completed imports after 5-second delay. Fixed application crash caused by corrupted InvoicePreview.tsx file with duplicate code blocks. Progress interface now correctly displays final import results (processed/successful/failed counts) instead of showing zeros after completion.
- January 30, 2025. Fixed RPA success/failure tracking issue: Resolved critical bug where RPA imports showed "10 / 0 / 0" (Total/Success/Failed) statistics due to improper success validation. Enhanced Python RPA service trigger_manual_processing method to check actual JSON response success field instead of just HTTP 200 status. Removed duplicate progress API endpoints that were causing response conflicts. Consolidated progress tracking to properly use both in-memory real-time updates from STATS output and database fallback. RPA imports now correctly track and display actual processing success and failure counts based on manual pipeline results rather than just download completion.
- January 30, 2025. Implemented comprehensive polling-based RPA progress tracking system: Created dedicated GET /api/rpa/progress/:jobId endpoint that returns real-time progress with status, stage, progressPercent, total, processed, success, and failed fields. Enhanced PythonInvoiceImporter class with logId-to-configId mapping for accurate progress retrieval during and after job execution. Updated ProgressTracker component to poll every 3 seconds using new RPA endpoint, converting progress data to existing UI format. Modified InvoiceImporter page to capture logId from import responses and automatically show progress tracker. Extended progress cleanup timeout to 2 minutes for reliable polling access. Fixed "Initializing..." UI stuck state by implementing proper real-time progress updates that reflect actual backend processing stages and statistics.
- January 30, 2025. Fixed RPA progress bar stuck at "Initializing..." issue: Enhanced Python RPA service to output STATS lines during all processing phases including early initialization (update_progress method), download phase (_output_download_progress method), and file processing phase (_output_progress_stats method). Removed conflicting old polling systems in InvoiceImporter that were causing 404 errors to old endpoints. Added comprehensive debug logging to Node.js STATS parsing to track when progress updates are received. Enhanced progress tracking to show real-time download progress (30-90% range) and file processing progress (90-100% range) with accurate current step descriptions and invoice counters throughout the entire RPA automation pipeline.
- January 30, 2025. Successfully resolved RPA progress tracking UI issues: Fixed application crash caused by port conflicts, implemented faster 1-second polling intervals for real-time updates, and verified complete progress system functionality. UI now correctly displays progress percentages (70-78%), current steps ("Downloading 8/10: FEPG793514"), and live statistics (Total/Processed/Success counts) during RPA import operations. Progress bar updates smoothly throughout entire automation pipeline from initialization through completion with comprehensive real-time feedback.
- January 30, 2025. Fixed Run Now button UI behavior: Removed unwanted progress tracking modal and implemented in-card progress bar updates for manual import triggers. Run Now button now directly updates the configuration card with real-time progress instead of showing loading modal. Enhanced logId mapping between API responses and progress tracking system to ensure consistent polling. Added fallback polling mechanism for reliable progress updates even when WebSocket connections fail. Modal interface completely removed for streamlined user experience with immediate visual feedback in the configuration card.
- January 30, 2025. Fixed RPA retry logic for failed invoices: Replaced simple duplicate detection with intelligent processing status check that only skips invoices successfully imported to main PostgreSQL invoices table. Failed downloads or processing attempts are now automatically retried on subsequent RPA runs. Added _is_invoice_successfully_processed method that queries main invoices table using document number and vendor matching to distinguish between failed attempts (retry) and successful imports (skip). This ensures failed invoices get another chance for processing while preventing duplicate imports of successfully processed invoices.
- January 30, 2025. Fixed data inconsistency in RPA import results summary UI: Corrected critical math error where Total count displayed totalInvoices (invoices found in ERP) instead of actual processed count (successful + failed). Changed display logic to show (successfulImports + failedImports) as Total across all UI locations including results table, in-card progress display, and console stats summary. Fixed inconsistent reporting where "10 / 8 / 4" showed impossible math (8+4≠10), ensuring Total = Success + Failed formula is always accurate and reflects actual invoice processing attempts rather than ERP discovery counts.
- January 30, 2025. Enhanced RPA duplicate detection logic for effective invoice skipping: Completely rewrote _is_invoice_successfully_processed method to use precise invoice number matching instead of unreliable filename pattern matching. Implemented two-tier detection system with exact vendor name matching and normalized fallback matching for name variations. Added company-based filtering to respect multi-tenant data isolation. Enhanced logging to show specific duplicate detection results with invoice IDs and filenames. System now correctly identifies and skips already imported invoices like FEPG793513, preventing duplicate imports that were previously occurring due to different filename patterns from ERP system.
- January 30, 2025. Implemented comprehensive dual-table duplicate detection for RPA imports: Enhanced duplicate checking to query both main invoices table (fully processed) and imported_invoices table (downloaded but not yet processed). Added three-tier detection system: exact main table match, imported_invoices filename matching, and alternative filename pattern detection. Implemented normalized vendor name comparison with safe filename generation. Added detailed debug logging showing exact match conditions when duplicates aren't found. System now prevents reprocessing of invoices that exist in either processing stage, ensuring true duplicate prevention across entire import pipeline.
- January 30, 2025. Completed processing status lifecycle management: Fixed critical RPA duplicate reprocessing issue by implementing comprehensive processing_status enum system (downloaded, processing, completed, failed) with proper status transitions throughout invoice processing pipeline. Enhanced duplicate detection to only skip invoices with processing_status = 'completed', allowing automatic retry of failed imports while preventing reprocessing of successfully completed invoices. Fixed method signature inconsistencies and removed duplicate function definitions in Python RPA service. Status tracking now properly distinguishes between download completion and full processing completion, ensuring failed invoices get retried while successful imports are permanently skipped.
- January 30, 2025. Fixed ZIP download timeout configuration bug: Resolved critical issue where custom ZIP download timeout values (like 10 seconds) were not being passed from Node.js to Python RPA service, causing imports to always use default 60-second timeout. Enhanced pythonConfig object in _executeImportProcess method to include zipDownloadTimeout field from database configuration. Added comprehensive logging to verify timeout values are passed correctly. ZIP download timeout customization now works properly, allowing users to optimize import performance based on their ERP system response times.
- January 30, 2025. Enhanced RPA duplicate detection with hierarchical matching logic: Improved _is_invoice_successfully_processed method to prioritize numero_documento and emisor matching over valor_total validation. Implemented smart valor_total handling that skips invoices even when valor_total is None, assuming same document/vendor combination represents same invoice. Added 0.01 threshold validation for valor_total when both input and database values are available. Enhanced logging to clarify when valor_total was not used for validation, preventing unnecessary reprocessing when total amounts are missing from ERP data.
- January 30, 2025. Enhanced file pairing logic with dynamic normalization for inconsistent prefixes: Implemented regex-based invoice ID extraction using pattern r'(\d{20,26})' to match XML and PDF files with different prefixes but same invoice ID (e.g., ad090045721800025000066e6.xml and fv090045721800025000066e6.pdf). Added extract_invoice_token method for normalized matching, enhanced _match_files_by_token with three-tier matching priority (normalized ID > exact token > fallback doc_tax), and improved logging to show when files are linked using normalized tokens. System now correctly pairs files regardless of filename prefix variations while maintaining backward compatibility with existing naming conventions.
- January 30, 2025. Implemented comprehensive processing status tracking system for RPA imports: Added processing_status enum (downloaded, processing, completed, failed) to imported_invoices table with automatic lifecycle tracking. Enhanced duplicate detection logic to properly distinguish between successfully completed invoices (skip) and failed imports (retry with cleanup). Updated Python RPA service with _update_imported_invoice_status method for real-time status updates during processing. Added status transitions: downloaded → processing → completed/failed, with automatic retry mechanism for failed invoices by cleaning up incomplete records. System now accurately tracks complete import lifecycle preventing duplicate processing of successful imports while enabling retry of failed attempts.
- January 30, 2025. Implemented configurable ZIP download timeout feature: Added zip_download_timeout column to invoice_importer_configs database table with constraints allowing 10-300 seconds range. Enhanced frontend create/edit configuration dialogs with timeout input field, validation, and proper form state management. Updated Python RPA service to use configurable timeout values from database configurations. Users can now set custom download timeouts (default 60 seconds, minimum 10 seconds) to handle varying ERP system response times and network conditions. Feature includes complete database schema synchronization, frontend-backend integration, and proper configuration persistence for both new and existing import configurations.
- January 30, 2025. Restored enhanced OCR logic for improved PDF processing quality: Implemented advanced image preprocessing with Sharp library including contrast enhancement, noise reduction, brightness optimization, and sharpening for crisp text edges. Enhanced Tesseract configuration with bilingual Spanish+English recognition, optimized page segmentation mode (AUTO_OSD), specialized character whitelist for Latin American invoices, and improved DPI settings. Added comprehensive text preprocessing with OCR error correction, Colombian invoice terminology fixes, enhanced number/date formatting, and structured section markers. Applied scope restriction ensuring enhanced OCR only processes PDF-only invoices while XML files maintain priority as sole data source. Successfully verified improved extraction quality with accurate vendor names and amounts from RPA-imported PDFs.
- January 30, 2025. Fixed RPA file matching issue causing 3 failed PDF imports: Diagnosed and resolved critical bug where XML+PDF invoice pairs with different filename patterns (e.g., "04VC122470_890107487.xml" vs "04VC122470_890107487_SUPERTIENDAS_Y_DROGUERIAS_OLIMPICA_SA.pdf") weren't being matched correctly. Enhanced file matching algorithm with two-pass system: exact base name matching followed by intelligent token-based matching using document_number_tax_id pattern extraction. Added _extract_invoice_token_from_filename method that extracts composite tokens from first two underscore-separated filename parts. Updated processing logic to properly handle matched pairs, unmatched XMLs, and unmatched PDFs with enhanced logging showing match types (exact/token). This ensures XML files take processing priority for data extraction while PDFs serve as visual references, preventing misclassification of XML files as PDF-only imports that would fail OCR processing.

## User Preferences

Preferred communication style: Simple, everyday language.